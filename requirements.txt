# Researcher AI Bot Dependencies
# Python 3.9+ supported (3.10+ recommended for latest LangGraph)
# Requires: ANTHROPIC_API_KEY environment variable

# LLM - Claude via Anthropic API
langchain-anthropic>=0.3.0
anthropic>=0.40.0

# Agent framework
langgraph>=0.2.0
langgraph-prebuilt>=0.6.0

# Core LangChain (messages, tools)
langchain-core>=0.3.0

# Web search via DuckDuckGo
ddgs>=6.0.0

# Wikipedia search (public API, no key)
wikipedia>=1.4.0

# Chat UI (optional)
streamlit>=1.28.0
